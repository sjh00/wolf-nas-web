# 版本历史

## v4.20.16 (2026-09-12)

### 修复

- 版本号与后端 v4.20.16 同步（本次为后端 v4.0.0 后迁移链 SQLite 兼容修复，前端无功能改动）
- v4.20.15 未单独发布前端，一并跳到 v4.20.16

## v4.20.14 (2026-09-12)

### 修复

- 版本号与后端 v4.20.14 同步（本次为后端迁移多库兼容修正，前端无功能改动）

## v4.20.12 (2026-09-12)

### 修复

- 版本号与后端 v4.20.12 同步（本次为后端迁移自愈，前端无功能改动）

## v4.20.10 (2026-09-12)

### 修复

- 版本号与后端 v4.20.10 同步（本次为后端迁移自愈，前端无功能改动）

## v4.20.9 (2026-09-12)

### 修复

- 版本号与后端 v4.20.9 同步（本次为后端类型判定/qB/刷流修复，前端无功能改动）

## v4.20.8 (2026-09-11)

### 修复

- 版本号与后端 v4.20.8 同步（本次为后端数据库迁移修复，前端无功能改动）

## v4.20.7 (2026-09-11)

### 修复

- 版本号与后端 v4.20.7 同步（本次为后端测试修正，前端无功能改动）

## v4.20.6 (2026-09-11)

### 修复

- 版本号与后端 v4.20.6 同步（本次前端无功能改动）

## v4.20.5 (2026-09-11)

### 修复

- 版本号与后端 v4.20.5 同步（v4.20.4 因发布事故作废；本次前端无功能改动）

## v4.20.3 (2026-09-11)

### 新增

- 近期下载支持删除单条记录与「清空全部」

### 修复

- 基础设置允许清空代理等非敏感项（空值保存生效），密码/密钥等敏感字段仍保留原值
- 修复订阅编辑弹窗类型定义缺少 history_id 导致的类型检查失败

## v4.20.2 (2026-09-11)

### 修复

- 版本号与后端 v4.20.2 同步（本次为后端 NFO 命名修复，前端无功能改动）

## v4.20.1 (2026-09-11)

### 修复

- 版本号与后端 v4.20.1 同步（本次为后端测试修正，前端无功能改动）

## v4.20.0 (2026-09-11)

### 新增

- **多用户数据权限前端支持**（配合后端 ADR-021）：站点授权管理与渠道绑定设置页；站点授权抽屉（角色/用户两级、搜索/RSS 用途粒度，超管只读全选）
- 管理员订阅聚合视图：聚合卡片重设计为海报风格、平铺卡片展示归属用户，聚合面板订阅行超 3 行折叠，归属标识改为标题下居中胶囊
- 资源搜索页新增可选索引器（站点多选）；订阅确认弹窗显示将使用的站点，配置入口更明确
- 推送渠道登记页按渠道动态展示 Key 获取指引

### 修复

- 默认订阅设置改用可见站点，站点授权入口按登录用户权限显隐
- 订阅聚合视图布局对齐平铺（flex 流），消除卡片间大片空白
- 渠道绑定页图标修正
- 消息中心打开时定位到第一条未读

## v4.19.7 (2026-09-09)

### 修复

- 资源搜索页移动端（≤900px）结果改为卡片式布局，不再横向截断；下载/详情按钮整行右对齐展示（触屏无 hover 也可见）；版本号与后端 v4.19.7 同步

## v4.19.6 (2026-09-09)

### 修复

- 版本号与后端 v4.19.6 同步（RSS 候选去重为后端变更，前端无功能改动）

## v4.19.5 (2026-09-09)

### 修复

- 自定义识别词保存：`group_type` 显式字符串化，避免数值类型导致 400；编辑/新增表单改用非动态删除，消除 lint 报错
- 版本号与后端 v4.19.5 同步

## v4.19.4 (2026-09-09)

### 修复

- 版本号与后端 v4.19.4 同步（站点测试登录态检测、autosignin 繁体判定/多调度去重/U2 时段为后端变更，前端无功能改动）

## v4.19.3 (2026-09-08)

### 修复

- 版本号与后端 v4.19.3 同步（后端为自定义识别词 PostgreSQL 兼容修复，前端无功能改动）

## v4.19.2 (2026-09-08)

### 修复

- 插件“已安装”口径统一：插件市场与已安装列表都以“已安装 或 已启用”为准，不再出现市场全已安装、列表却为空的不一致；修复插件市场多处 lint 问题

## v4.19.1 (2026-09-08)

### 修复

- 版本号与后端 v4.19.1 同步（详情页抓取退避、备份恢复兼容、NEXUS_PORT 同步均为后端变更，前端无功能改动）

## v4.19.0 (2026-09-08)

### 功能

- PWA 与浏览器系统通知：Service Worker + 图标/清单，接收后端 Web Push 弹系统通知，点击跳转消息中心
- 首页仪表盘增强：上传/下载 KPI 新增“今日增量”趋势徽标（与电影/电视剧口径一致）；下载器负载显示实时上下行速率，配置限速后展示占用进度条与百分比
- 与后端 v4.19.0 同步（订阅 add_date、解析健康仓储重构等）

### 修复

- 首页 KPI 图标背景统一主题变量；上传/下载数值与趋势小字对齐，卡片高度不再被撑高

## v4.18.0 (2026-09-07)

### 功能

- 站点维护页新增「解析自检」：后台异步执行 + 状态轮询刷新，不再超时；健康汇总条默认折叠，页面改版（结构异常）与凭据失效分组展示；异常站点可一键复制反馈到 nexus-media-sites
- 与后端 v4.18.0 同步（解析健康监控、种子属性收敛等）

## v4.17.1 (2026-09-07)

### 修复

- 版本号与后端 v4.17.1 同步；刷流免费种子误删与观众做种数解析等修复由后端处理，前端无功能变更

## v4.17.0 (2026-09-07)

### 功能

- 首页看板重设计：KPI 卡整卡可点击跳转对应页面（键盘可达）并带环比趋势徽标（电影/电视剧按剧名口径、上传/下载今日增减）；新增系统状态卡（CPU/内存环形仪表 + 存储与下载器负载进度条，30s 轮询）；入库趋势 7/30/90 天切换；最近入库整行海报墙；所有卡片配色统一 Tabler 主题变量，明暗主题一致
- 站点数据统计页重设计：站点多时各图表 TopN 截断展示并标注（流量对比 Top 12、分布 Top 8）；跨图统一站点颜色映射（同一站点在所有图表/图例/曲线中同色）；修复暗色模式下图例/坐标轴/tooltip 文字；图例长站名截断；今日流量卡与图表卡样式统一；汇总卡语义色 + 响应式网格；近 7 天/30 天零增量站点自动过滤，移动端图表单列布局

### 修复

- 近 7 天流量增量在全 0 数据时不再渲染空柱图；30 天趋势过滤全程零增量站点，站点多时默认聚焦流量 Top 5

## v4.16.1 (2026-09-06)

### 修复

- 版本号与后端 v4.16.1 同步；刷流删种/停种到期语义（Free 到期才删/停）与重复进种去重由后端规则引擎处理，前端无功能变更

## v4.16.0 (2026-09-05)

### 功能

- 插件市场页完整落地：官方源一键添加/市场源管理、目录浏览与详情懒加载、SAST 安装预检后安装/更新；本地（内置/第三方）与远程市场统一浏览
- 分类/标签筛选聚合插件元数据（主分类 + 被多个插件共用的常用标签按频次），标签配色统一使用主题 `--tag-*` 语义色
- 系统设置-日志新增"日志轮转大小 / 日志保留天数"配置项（需与后端 4.16.0 同步）

### 修复

- 移动端侧栏恢复 vben 模板抽屉行为：logo 旁图标展开菜单抽屉并带遮罩，折叠交互与官方模板一致
- 系统日志页选择来源/级别/关键词后不再显示"共 0 条"（24h 磁盘扫描超时），改为 mmap 快速检索；结果过多时截断并提示
- 已安装插件页名称截断与启用开关在窄屏下不再溢出

## v4.15.2 (2026-09-03)

### 修复

- 移动端侧栏与桌面行为完全一致：侧栏常驻宽度跟随"折叠菜单"偏好切换（60/224），折叠态仅保留折叠按钮
- 移动端侧栏 DOM 渲染跟随偏好：仅 hover 展开时渲染，避免折叠状态残留占用布局
- 系统日志移动端保持多列表格并横向滚动，避免内容被挤成一列

## v4.15.1 (2026-09-03)

### 功能

- 识别历史卡片改版：显示海报（后端按 TMDBID 富化），源/目的后端徽标 + 完整路径展示，超长路径悬停显示全文

### 修复

- 移动端侧栏抽屉展开宽度遵循"折叠菜单"偏好：折叠开启时按窄栏（60px）渲染，不再出现"全宽+纯图标"错位
- 移动端侧栏底部按钮与桌面态一致：折叠态仅保留折叠按钮，展开态显示图钉+折叠按钮，不再重叠

## v4.15.0 (2026-09-03)

### 变更

- 已下载/下载历史接口透传 page_size（默认 30，上限 200），适配后端分页能力
- 站点维护页统一筛选栏（来源/类型/搜索），隐藏 0 来源站点，功能开关网格自适应并六项默认开启
- 版本号与后端同步至 4.15.0（后端：站点抓取"直连优先→chrome 降级"与会话清理、索引器搜索超时环境变量化等）

## v4.14.1 (2026-09-03)

### 变更

- 版本号与后端同步至 4.14.1（后端补丁：Rousi 签到失败原因记录 + HR 规则误停修复，前端无功能改动）

## v4.14.0 (2026-09-02)

### 变更

- 头像在线圆点与消息角标缩小、统一圆角；移动端顶部隐藏 logo（侧边栏抽屉内保留）
- 新增消息通知/下载器类型选择改为等宽自适应，消除移动端两列留白与末行宽度不一
- 无配置字段的插件隐藏"配置"入口，配置弹窗空状态提示（不显示保存）
- 偏好设置按钮收拢至用户下拉菜单

## v4.11.10 (2026-08-31)

### 变更

- 版本号与后端同步至 4.11.10（本次为后端修复：转移-刮削解耦异步化 + 图片下载限速，前端无功能改动）

## v4.11.9 (2026-08-31)

### 修复

- **刷流任务表单旧规则下拉显示 id**：`type='all'` 的旧规则（TYPE 列迁移前创建）被硬过滤导致下拉无法显示名称，改为按规则内容推断类型（选种/删种/停种），与规则管理页逻辑一致；类型推断逻辑抽取为共享工具
- **前端请求超时 10s→60s**：适配多站搜索、下载编排、文件同步等耗时操作，减少偶发"请求超时"

## v4.11.8 (2026-08-30)

### 变更

- 版本号与后端同步至 4.11.8（本次为后端修复：刷流已处理种子缓存改为 TTL 过期，优惠等时变状态变化后可重新评估，前端无功能改动）

## v4.11.7 (2026-08-30)

### 修复

- **默认密码登录无限重定向卡死**：强制改密跳转（`is_default_password`）在动态路由注册前执行，`/profile` 无法解析落入 404 导致守卫无限循环、浏览器卡死/OOM。已将该跳转移至路由生成完成后，登录后正常跳转改密页

## v4.11.6 (2026-08-30)

### 变更

- 版本号与后端同步至 4.11.6（本次为后端修复：刷流任务加载容错、num_filesize 非大小文本不报错，前端无功能改动）

## v4.11.5 (2026-08-30)

### 变更

- 版本号与后端同步至 4.11.5（本次为后端修复：RBAC 初始化懒加载报错，前端无功能改动）

## v4.11.4 (2026-08-30)

### 新增

- 实验室设置新增「访问凭证（API Key）」配置项：nexus-chrome 启用 `AUTH_PASSWORD` 认证后，浏览器自动化请求需携带该凭证

### 修复

- **访问凭证无法保存**：实验室保存字段白名单缺少 `laboratory.chrome_admin_token`，导致填写后保存无效（后端已支持热重载）

## v4.11.3 (2026-08-29)

### 修复

- **图表渲染无限递归导致浏览器内存暴涨/OOM**：`useEcharts` 在图表元素隐藏时每 30ms 无限递归渲染，promise 链无限累积（部分用户 Edge 报 Out of memory、Chrome 一直加载后崩溃）。限制重试次数（600ms），隐藏期间放弃；元素恢复可见时补齐渲染

## v4.11.2 (2026-08-29)

### 变更

- 版本号与后端同步至 4.11.2（本次为后端修复：刷流任务重启丢失、站点统计精确化、刷新时间多时间点，前端无功能改动）

## v4.11.1 (2026-08-29)

### 变更

- 版本号与后端同步至 4.11.1（本次为后端修复：观众签到误报失败、未配置 nexus-chrome 指纹同步误报，前端无功能改动）

## v4.11.0 (2026-08-28)

### 特性

- **数据统计页**：新增今日流量卡片（占比环形图 + 分站点排行）；站点统计图表联动（点击柱状图/饼图/玫瑰图/趋势线聚焦站点，其余置灰）；近7天/近30天流量趋势筛选（多选）与曲线选中；站点详细数据支持站点多选筛选
- **转移弹窗**：携带源存储后端标识，选择后端后目录选择器直接跳转对应后端
- **存储后端表单**：布尔字段改用开关，启用开关移至头部
- **刷流任务**：保存目录支持从所选下载器目录中选择

### 修复

- **趋势图置灰**：折线透明度改用 lineStyle.opacity 控制，hover 后曲线不再消失；积分曲线独立青色；点击事件改用 zrender 底层绑定
- **今日流量卡片**：环形图边框过粗、分享率无限显示 99、昨日为 0 时增长显示 +0.0% 等

## v4.10.2 (2026-08-27)

### 说明

- 版本号与后端 v4.10.2 同步（无功能改动，后端为站点配置更新源可配置、站点 id 统一小写及数据迁移、站点配置 schema 完善）

## v4.10.1 (2026-08-27)

### 说明

- 版本号与后端 v4.10.1 同步（无功能改动，后端为 WebDAV/同步监控/YemaPT 适配修复）

## v4.10.0 (2026-08-27)

### 修复

- **夜间模式 Naive UI 组件显示异常**：auto 模式（跟随系统深色）下 Naive UI 基础主题按字面 `mode` 判定，系统深色时 CSS 变量已切暗而弹窗内输入框仍为白底；统一按实际生效的暗色解析（与 `.dark` 类切换同源），弹窗、对话框等 discrete API 组件同步修复

## v4.7.0 (2026-08-18)

### 特性

- **站点高级请求头 KV 编辑**：改为键值两列（增删行）+ JSON 批量模式切换
- **首页索引器统计组合图**：成功/失败柱状 + 平均耗时折线（双轴），悬停展示调用/成功率/耗时完整指标

### 修复

- **消息中心时间线排序**：会话与通知时间单位统一（秒→毫秒），按时间交错排列（此前会话永远置顶、刷新后只见通知）

## v4.6.3 (2026-08-16)

### 修复

- **订阅进度显示**：后端订阅续订断点推导与 `current_ep` 推进修复，前端进度显示与实际转移同步

## v4.6.2 (2026-08-14)

### 修复

- **消息中心单图通知**：改为企业微信图文卡片样式——图片横幅在上、文字区在下，固定最大宽度 420px，替代图片单独一张的割裂展示
- **订阅刷新状态**：订阅卡片点击"刷新"后轮询拉取列表（最多 5 次、间隔 1.5s），"搜索中"状态及时显示，无需手动刷新页面；加 refreshing 防重入避免反复点击触发多个轮询

## v4.6.1 (2026-08-14)

### 特性

- **消息通知实时化**：铃铛改为 SSE 实时推送（新消息即时更新未读徽标与下拉，不再依赖轮询）；布局与消息中心共用通知工具（OS 通知聚合、提示音、点击跳转、惰性权限请求）
- **精准已读**：消息中心改用视口内已读（IntersectionObserver），消息进入视口停留片刻才标记该条已读，替代定时全量已读
- **跨标签未读同步**：BroadcastChannel + window 事件，多标签页未读数实时一致
- **通知设置**：铃铛下拉新增系统通知 / 提示音开关，本地持久化

### 修复

- 消息中心：图片加载失败自动隐藏破损图（不占位）
- 通知去重：仅真正弹出系统通知时才记录消息 ID，前台到达不误占去重位

## v4.6.0 (2026-08-13)

### 特性

- **消息中心（AI 助手）**：对话式操作全系统——思考过程可折叠面板（推理文本实时流式显示）、工具调用步骤与状态、危险操作确认卡片、回答 markdown 渲染
- **知识库页面**：重建索引、命名空间查看
- Agent 对话 API（SSE 跨块缓冲解析，防止事件被网络分块拆散丢失）
- Agent 设置页新增 Embedding 模型列表与通知增强（notify）配置
- 统一错误码提示（对齐后端）

### 修复

- 消息中心：中文输入法组合状态回车不误发消息（isComposing 判定）
- 下载历史：复制链接在非安全上下文/剪贴板权限被拒时降级 execCommand 兜底
- 站点编辑：优先级文案修正为「数值越小优先级越高，主站建议 1」

## v4.5.0 (2026-08-08)

### 特性

- 重命名：新增独立的「重命名」配置页签，芯片式格式构建器——变量池点击/拖拽添加、画布拖拽排序与删除、固定文本块双击内联编辑、分隔符控制、实时预览（电视剧《怪奇物语》/电影《盗梦空间》示例）、预设模板、生成的格式字符串可编辑并双向同步；电视剧/电影独立编辑
- 浏览器指纹：登录成功同步真实浏览器指纹，后台流程（站点定时刷新 / RSS 自动化等）复用该指纹

### 修复

- 媒体：重命名格式字段回归普通表单输入，格式编辑统一收敛到「重命名」页签

## v4.4.5 (2026-08-04)

### 特性

- 文件管理：页面对齐真实文件管理器交互重设计

### 修复

- 订阅：搜索页/媒体详情页订阅确认弹窗「编辑」按钮无反应，现正确打开订阅编辑弹窗并预填默认订阅设置
- 订阅：编辑弹窗先加载站点选项再填表单，修复默认订阅站点（rss_sites/search_sites）被空选项过滤清空

## v4.4.4 (2026-08-03)

### 特性

- 下载：自动删种任务页面整体重构，与刷流规则页风格统一——卡片式布局、动作类型彩色 chip（暂停/删除/删除及文件差异化配色）、过滤条件标签按类型差异化配色（tabler-theme `--tag-*` 语义色）
- 下载：删种任务卡片底部操作栏——执行/预览/编辑带文字标签按钮、危险色删除按钮，启用状态开关快捷切换（无需进入编辑弹窗）
- 下载：删种任务新增/编辑弹窗改为分组网格表单（基本信息/数值条件/匹配条件），字段附帮助提示，开关替代下拉选择，移动端单列自适应

## v4.4.3 (2026-08-03)

### 说明

- 版本号与后端同步至 4.4.3，本轮无前端功能性改动

## v4.4.2 (2026-08-02)

### 特性

- 系统：服务面板新增「缓存管理」弹窗，列出各缓存键数/占用/命中率统计，支持单个或全部清理（对接 `GET/POST /system/caches`）
- 系统：服务面板整体重构，使用 `tabler-theme.css` 颜色系统——统计卡片加彩色图标、服务卡片按类别差异化配色、Tabler 阴影/圆角/过渡统一的卡片视觉
- 搜索：名称识别测试 `nameTestApi` 支持 `subtitle` 参数，传资源描述提高多语言标题命中率

### 修复

- 服务：缓存管理弹窗改为内联 NModal `v-model:show`（修复子组件 `:show` 导致弹窗内容内联渲染在页面底部而非浮动模态框）
- 系统：`discovery/index.vue` 推荐项跳转搜索页字段 `mediaType` → `media_type`，修复类型字段引用错误
- 系统：订阅（影视/剧集）页面移除未使用的 `startSearchSSE` 解构，修复 typecheck 报错

## v4.4.1 (2026-08-01)

### 特性

- 站点资源：资源列表每个资源新增「识别」按钮，点击弹窗显示完整 TMDB 识别结果（名称/tmdbid/海报/季集/年份/资源质量）
- 搜索：跳转搜索页支持传递 `media_type`，发现/详情/订阅页进入按媒体类型定向搜索

### 修复

- 未识别列表：识别弹窗改为先打开再请求（加载动画可见），修复「识别测试」加载期间无加载动画
- 发现页：`RecommendItem` 跳转搜索页字段 `mediaType` → `media_type`，修复类型字段引用错误
- 订阅（影视/剧集）：移除未使用的 `startSearchSSE` 解构，修复 typecheck 报错
